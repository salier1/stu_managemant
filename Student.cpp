#include "Student.h"

Student::Student(unsigned int number, const std::string& name, const std::string& grade, unsigned int math, unsigned int chinese, unsigned int english)
	:number(number),name(name),grade(grade),math(math),chinese(chinese),english(english)
{
}
